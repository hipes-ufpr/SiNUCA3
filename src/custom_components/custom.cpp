#include "../sinuca3.hpp"
#include "custom_example.hpp"

// clang-format off
COMPONENTS(
    // Each custom component added should be declared here just like this one.
    COMPONENT(CustomExample);
    //Component(MyCustomCache);
    //Component(MyCustomPredictor);
    // etc.
)
// clang-format on
